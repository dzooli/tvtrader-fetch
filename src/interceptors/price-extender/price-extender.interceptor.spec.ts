import { PriceExtenderInterceptor } from './price-extender.interceptor';

describe('PriceExtenderInterceptor', () => {
  it('should be defined', () => {
    expect(new PriceExtenderInterceptor()).toBeDefined();
  });
});
